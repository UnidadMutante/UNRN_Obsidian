
crear acceder operar imprimir vectoresy matrices estaticos
float matrix \[ n \] [m] 

crear acceder operar imprimir vectores y matrices dinamicos

float * matrix = malloc (N * M * sizeof(float))

que otra forma de crear una matroz

operaciones con cadenas de carácteres. cadenas seguras. 

pensar un algoritmo para resolver un problema

documentacion params pre y post

modularizar en funciones

argumentos


ojo con el pasaje de matrices por copia



el largo de la cadena no es lo mismo que la capacidad de la cadena

si declaro char a [9] = "hola";
el largo de la cadena es la cantidad de letra
y la capacidad es el espacio que tengo reservado

while(a[i] != '\0') && (i < capacidad) {
}

**cadena segura es**: una cadena que esta definida en un espacio del que conozco la capacidad puede o no tener el barra 0
cuando opero con cadena segura, paso como parametrp la capacidad y uso la capacidad para limitar los ciclos

![[Pasted image 20260915155647.png]]


