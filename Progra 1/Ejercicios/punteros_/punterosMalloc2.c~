# include <stdio.h>

int main(void)
{
	char str[] = "hola personas";
	char *ptr = str;
	char *ptr2 = str;
	printf("Caracteres de la cadena: ");
	

	int i = 0;
	while(ptr[i] != '\0') {
		printf("%c", ptr[i]);
		i++;
	}
	printf("\n");

// otra forma de hacer lo mismo
	while(*ptr != '\0') {

	printf("%c", *ptr);
	ptr++;
	}
	printf("\n");
	return 0;
}
